describe('editProps', () => {

  // it('should make a make a prop output container', () => {
  //   const e = propOutput('test')
 
  //   //console.log(e)
    
  //   expect(e.getElementsByClassName('activeItemTitle')[0].innerHTML).toBe('test')
  //   //expect(e.getElementById('prop')).not.toBe('undefined')
  // })

  // it('should make a new prop button', () => {
  //   let el = addNewProp()
  //   expect(el.innerHTML).toBe('New Prop')
  // })

  // it('should make a new attribute text field', () => {
  //   const el = addNewAttribute()

  //   console.log((el))
    
  //   //edit('new')
  //   expect(1).toBe(2)
  // })
})